# Результаты нагрузочного тестирования
## Инструмент: Apache Benchmark (ab)
## Параметры: 1000 запросов, 10 параллельных соединений

| # | Тест | Req/sec | Время (ms) |
|---|------|---------|------------|
| 1 | Статика напрямую через nginx | 258 | 38 |
| 2 | Статика напрямую через gunicorn | 100 | 99 |
| 3 | Динамика напрямую через gunicorn | 91 | 109 |
| 4 | Динамика через nginx → gunicorn (без кеша) | 4406 | 2 |
| 5 | Динамика через nginx → gunicorn (с proxy_cache) | 7265 | 1 |

## Выводы

### Насколько быстрее отдается статика по сравнению с WSGI
Nginx отдаёт статику в **2.5 раза быстрее** чем gunicorn
(258 req/sec против 100 req/sec).

### Во сколько раз ускоряет работу proxy_cache
proxy_cache ускоряет работу в 1.6 раза
(7265 req/sec с кешем против 4406 req/sec без кеша).
По сравнению с прямым обращением к gunicorn (91 req/sec) —
nginx с proxy_cache быстрее в 80 раз.